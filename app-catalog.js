function openMaterialModal(id = '') {
  const material = state.materials.find(item => item.id === id) || {};
  openModal(id ? 'Editar material' : 'Nuevo material', `<form id="materialForm" class="modal-form"><input type="hidden" name="id" value="${esc(material.id || '')}"><label>Nombre<input name="name" required value="${esc(material.name || '')}" placeholder="Ej. Taza blanca 11 oz"></label><label>SKU o clave<input name="sku" value="${esc(material.sku || '')}"></label><label>Categoría<input name="category" value="${esc(material.category || '')}" placeholder="Sublimación, papel, tinta..."></label><label>Unidad<input name="unit" value="${esc(material.unit || 'pieza')}" placeholder="pieza, metro, hoja, litro"></label><label>Existencia inicial<input name="stock" type="number" step="0.001" value="${num(material.stock)}" ${id ? 'readonly title="Usa Ajustar existencia para modificarla"' : ''}></label><label>Existencia mínima<input name="minStock" type="number" step="0.001" value="${num(material.minStock)}"></label><label>Costo unitario<input name="unitCost" type="number" min="0" step="0.01" value="${num(material.unitCost)}"></label><label>Proveedor<select name="supplierId">${supplierOptions(material.supplierId)}</select></label><label class="full">Ubicación / notas<textarea name="notes" rows="3">${esc(material.notes || '')}</textarea></label></form>`, `<button class="button secondary" data-close-modal>Cancelar</button><button class="button primary" form="materialForm">Guardar material</button>`);
}
function saveMaterial(form) {
  const data = Object.fromEntries(new FormData(form));
  const existing = state.materials.find(item => item.id === data.id);
  const material = { id: data.id || uid('material'), name: data.name.trim(), sku: data.sku.trim(), category: data.category.trim(), unit: data.unit.trim(), stock: existing ? num(existing.stock) : num(data.stock), minStock: num(data.minStock), unitCost: num(data.unitCost), supplierId: data.supplierId, notes: data.notes.trim(), updatedAt: new Date().toISOString() };
  const index = state.materials.findIndex(item => item.id === material.id);
  if (index >= 0) state.materials[index] = { ...state.materials[index], ...material }; else { state.materials.push({ ...material, createdAt: new Date().toISOString() }); if (material.stock) addInventoryMovement(material.id, material.stock, 'entrada', 'Existencia inicial'); }
  closeModal(true); saveState(index >= 0 ? 'Material actualizado' : 'Material agregado');
}
function openInventoryAdjustment(materialId = '') {
  openModal('Ajustar existencia', `<form id="adjustmentForm" class="modal-form"><label class="full">Material<select name="materialId" required>${materialOptions(materialId)}</select></label><label>Fecha<input name="date" type="date" value="${todayISO()}" required></label><label>Movimiento<select name="direction"><option value="add">Entrada / sumar</option><option value="subtract">Salida / restar</option><option value="set">Establecer existencia</option></select></label><label>Cantidad<input name="quantity" type="number" min="0" step="0.001" required></label><label>Motivo<input name="reason" required placeholder="Inventario físico, merma, devolución..."></label></form>`, `<button class="button secondary" data-close-modal>Cancelar</button><button class="button primary" form="adjustmentForm">Aplicar ajuste</button>`);
}
function saveAdjustment(form) {
  const data = Object.fromEntries(new FormData(form)); const material = state.materials.find(item => item.id === data.materialId); if (!material) return showToast('Selecciona un material', 'error');
  const before = num(material.stock); let delta = num(data.quantity); if (data.direction === 'subtract') delta *= -1; if (data.direction === 'set') delta = num(data.quantity) - before;
  material.stock = before + delta; addInventoryMovement(material.id, delta, 'ajuste', data.reason.trim(), '', data.date); closeModal(true); saveState('Existencia actualizada');
}

function recipeRow(row = {}) {
  return `<div class="line-row recipe"><select class="recipe-material">${materialOptions(row.materialId)}</select><input class="recipe-qty" type="number" min="0" step="0.001" value="${num(row.qty)}" placeholder="Cantidad"><span class="recipe-cost">${money(num(row.qty) * num(state.materials.find(item => item.id === row.materialId)?.unitCost))}</span><button type="button" class="action-button remove-row">×</button></div>`;
}
function openProductModal(id = '') {
  const product = state.products.find(item => item.id === id) || { salePrice: 0, recipe: [] };
  openModal(id ? 'Editar producto y costos' : 'Nuevo producto y costos', `<form id="productForm" class="modal-form"><input type="hidden" name="id" value="${esc(product.id || '')}"><div class="form-section"><div class="section-title"><div><h3>Información de venta</h3></div></div><div class="modal-form"><label class="full">Producto<input name="name" required value="${esc(product.name || '')}"></label><label>Categoría<input name="category" value="${esc(product.category || '')}"></label><label>Precio de venta<input name="salePrice" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.salePrice)}"></label><label>IVA informativo %<input name="taxPercent" type="number" min="0" step="0.01" value="${num(product.taxPercent)}"></label></div></div><div class="form-section"><div class="section-title"><div><h3>Receta de materiales</h3><p>Se descontarán automáticamente al poner un pedido en proceso.</p></div><button type="button" class="button secondary small" id="addRecipeRow">+ Material</button></div><div class="dynamic-list" id="recipeRows">${(product.recipe || []).map(recipeRow).join('')}</div></div><div class="form-section"><div class="section-title"><div><h3>Otros costos por unidad</h3></div></div><div class="modal-form"><label>Mano de obra<input name="laborCost" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.laborCost)}"></label><label>Diseño<input name="designCost" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.designCost)}"></label><label>Electricidad / gas<input name="electricityCost" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.electricityCost)}"></label><label>Empaque<input name="packagingCost" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.packagingCost)}"></label><label>Transporte<input name="transportCost" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.transportCost)}"></label><label>Trabajo externo<input name="externalCost" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.externalCost)}"></label><label>Otros costos<input name="extraCost" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.extraCost)}"></label><label>Desperdicio %<input name="wastePercent" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.wastePercent)}"></label><label>Comisión de cobro %<input name="commissionPercent" class="product-cost-input" type="number" min="0" step="0.01" value="${num(product.commissionPercent)}"></label><label class="full">Notas<textarea name="notes" rows="3">${esc(product.notes || '')}</textarea></label></div></div><div class="summary-box"><div class="summary-row"><span>Materiales</span><strong id="productMaterialPreview">$0.00</strong></div><div class="summary-row"><span>Costo total</span><strong id="productCostPreview">$0.00</strong></div><div class="summary-row total"><span>Ganancia</span><strong id="productProfitPreview">$0.00</strong></div></div></form>`, `<button class="button secondary" data-close-modal>Cancelar</button><button class="button primary" form="productForm">Guardar producto</button>`, true);
  updateProductCostPreview();
}
function updateProductCostPreview() {
  const form = $('#productForm'); if (!form) return;
  const recipe = $$('.line-row.recipe', form).map(row => ({ materialId: $('.recipe-material', row).value, qty: num($('.recipe-qty', row).value) })).filter(row => row.materialId && row.qty > 0);
  $$('.line-row.recipe', form).forEach(row => { const material = state.materials.find(item => item.id === $('.recipe-material', row).value); $('.recipe-cost', row).textContent = money(num($('.recipe-qty', row).value) * num(material?.unitCost)); });
  const values = Object.fromEntries(new FormData(form)); const preview = productBreakdown({ ...values, recipe }, values.salePrice);
  $('#productMaterialPreview').textContent = money(preview.material); $('#productCostPreview').textContent = money(preview.total); $('#productProfitPreview').textContent = money(preview.profit); $('#productProfitPreview').className = preview.profit < 0 ? 'money-negative' : 'money-positive';
}
function saveProduct(form) {
  const data = Object.fromEntries(new FormData(form));
  const recipe = $$('.line-row.recipe', form).map(row => ({ materialId: $('.recipe-material', row).value, qty: num($('.recipe-qty', row).value) })).filter(row => row.materialId && row.qty > 0);
  const product = { id: data.id || uid('product'), name: data.name.trim(), category: data.category.trim(), salePrice: num(data.salePrice), taxPercent: num(data.taxPercent), recipe, laborCost: num(data.laborCost), designCost: num(data.designCost), electricityCost: num(data.electricityCost), packagingCost: num(data.packagingCost), transportCost: num(data.transportCost), externalCost: num(data.externalCost), extraCost: num(data.extraCost), wastePercent: num(data.wastePercent), commissionPercent: num(data.commissionPercent), notes: data.notes.trim(), updatedAt: new Date().toISOString() };
  const index = state.products.findIndex(item => item.id === product.id); if (index >= 0) state.products[index] = { ...state.products[index], ...product }; else state.products.push({ ...product, createdAt: new Date().toISOString() });
  closeModal(true); saveState(index >= 0 ? 'Producto actualizado' : 'Producto agregado');
}
